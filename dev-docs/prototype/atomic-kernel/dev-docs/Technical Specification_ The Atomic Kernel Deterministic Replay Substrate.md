> Status: Legacy long-form background.  
> Canonical deterministic replay contract for integration: `docs/concepts.md`.

### Technical Specification: The Atomic Kernel Deterministic Replay Substrate

##### 1\. The Mathematical Foundation: Prime Factorial Timing and Generators

To eliminate the inherent entropy of environment-based clocks, the Atomic Kernel replaces volatile hardware interrupts with a timing kernel derived from pure mathematical generators. In distributed systems, relying on system time leads to non-deterministic state drift and "phantom" race conditions. By shifting the timing substrate to a discrete progression of modular phases, we ensure that every node in the network achieves a "byte-stable" state, making implementation-complete verification possible across heterogeneous hardware.The timing substrate is built upon a 7-cycle deterministic tick governed by a 7-bit closure algebra. We utilize prime factorials—specifically the prime nature of 7—to define the generator's state space. Because 7 is prime, its periodicity remains non-overlapping with standard power-of-two computer cycles (16, 32, 64-bit), effectively preventing harmonic aliasing and ensuring that the logical "tick" sequence remains unique within its modular field. The generator operates over a finite modular space where time is a function of the closure algebra rather than an observation of a quartz crystal.This transition from interrupts to mathematical generators is the prerequisite for deterministic replay. By decoupling the system from CPU frequency and network latency, the kernel generates a fixture corpus that is invariant across all compliant runtimes. This foundation ensures that any deviation in output is treated as a mechanical failure of the runtime implementation rather than an environmental anomaly.This mathematical rigor at the temporal level provides the necessary stability to project the complex multi-dimensional structural topology required by the higher-level manifest.

##### 2\. Structural Topology: Coxeter Notation and Dimensional Expansion

The 32-layer manifest is not a linear software stack; it is a multi-dimensional expansion governed by Coxeter notation. Coxeter groups provide the formal symmetry models required to describe how the system "folds" from the singular Ground Truth of the Pure Algorithmic Kernel into the broader domains of the Personal Metaverse. Coxeter notation defines the reflection-based relationships between layers, ensuring that the properties of the core are preserved as the system expands in complexity.In this model, the "Pure Algorithmic Kernel" (Layers 01–08) serves as the fundamental simplex from which the rest of the manifest is projected. The symmetry groups defined by the Coxeter notation ensure that even as we reach the "Personal Metaverse" (Layers 26–31), every operation remains constrained by the original reflections of the kernel law. This structural topology enforces the rule that while the manifest may expand to describe vast organizational data, it cannot break the underlying symmetry of the deterministic core.The Authority Ladder provides the normative hierarchy for this expansion, distinguishing between the foundational Ground Truth and the advisory projections:| Layer Group | Dimensional Domain | Status | Authority Type || \------ | \------ | \------ | \------ || **Core Law** | Publication I (Kernel/Coq) | **Normative** | **Ground Truth (Constitutional)** || **01–08** | Pure Algorithmic Kernel | Advisory | Extension (Summarized) || **09–16** | Aztec Encoded Runtime | Advisory | Extension || **17–25** | Distributed Identity API | Advisory | Extension || **26–31** | Personal Metaverse | Advisory | Extension || **32** | Full Manifest Composition | Advisory | Projection |  
This multi-dimensional expansion ensures that the higher layers serve as descriptive projections of the kernel core, remaining strictly bound by the mathematical invariants of the Ground Truth.

##### 3\. The Normative Kernel Law: Delta and Replay Mechanics

The freezing of the deterministic kernel core in Publication I is essential for mechanized verification. Publication I serves as the  **Coq Companion** , providing a formal contract in the Coq language (AtomicKernel.v). While the Python runtime (atomic\_kernel.core) is the executable reference, the Coq file is the ultimate arbiter of truth. If the runtime behavior diverges from the mechanized proofs, the runtime is classified as broken and must be corrected to achieve parity with the formal contract.The Normative Law defines state transitions across supported bit-widths (16, 32, 64, 128, 256\) through the delta\_n(x) function:$$\\text{delta}\_n(x) \= \\text{mask}\_n(\\text{rotl}\_n(x,1) \\oplus \\text{rotl}\_n(x,3) \\oplus \\text{rotr}\_n(x,2) \\oplus C\_n)$$Where  $C\_n$  is the byte pattern 0x1D repeated to the target width. The replay(seed, k) mechanism is defined as the iterative application of  $\\text{delta}\_n$  for  $k$  steps. The mask\_n operation is not merely a utility; it is the mechanism that guarantees the  **idempotence**  proven in Coq, ensuring that states never exceed their defined bit-width and that overflows are handled deterministically.The Verification Triad enforces this law through three pillars:

* **Proofs:**  Mechanized core theorems in Coq confirming the determinism of delta and replay orbits.  
* **Gates:**  Wave gate suites (atomic-kernel-gate.sh) that provide runtime enforcement of these mathematical laws.  
* **Replay Locks:**  Reproducibility checks where the "replay-hash" of a fixture corpus must match the golden artifact exactly.These deterministic transitions generate the raw sequence data necessary to derive unique, verifiable system identities.

##### 4\. Identity and Occurrence: SID, OID, and the Deterministic Clock

A distributed architecture requires the absolute separation of semantic identity (the essence of an object) from occurrence identity (its specific instantiation in time). Semantic Identity (SID) defines "what" an object is, while Occurrence Identity (OID) defines "when" it happened. This separation prevents the ambiguity common in event-sourced systems.Identity derivation follows a strict SHA256 logic:

1. **Semantic Identity (SID):**  SID \= sha256(type \+ canonical\_form)  
2. **Occurrence Identity (OID):**  OID \= sha256(clock\_position \+ sid \+ prev\_oid)The OID creates an append-only hash-chain where each occurrence is mathematically linked to its predecessor and the specific clock\_position. The Clock Law defines deterministic bounds for this position: Frames (0–239), Ticks (1–7), and Control (0–59). Crucially, the Clock Law synchronizes with the prime-factorial timing mentioned in Section 1; the 7-cycle tick of the clock is a high-level representation of the 7-bit closure algebra of the underlying generator.This dependency chain creates a  **fail-closed validation gate** . Because an OID cannot be computed without the correct prev\_oid and a valid clock\_position, identity forgery is mathematically impossible without breaking the replay lock. If any element—the semantic form, the clock frame, or the link reference—is altered, the OID will fail to recompute, and the system will reject the artifact.

##### 5\. The Enforcement Plane: Gates, Rituals, and Authority Boundaries

The Enforcement Plane secures the substrate by applying the Authority Separation principle. This principle forbids projection layers from ever escalating their authority to mutate the kernel state. Operational integrity is maintained through rigorous rituals and defined script roles that act as hard boundaries for state changes.Operational rituals are partitioned as follows:

* **Verification Ritual:**  Conformance is validated via conformance/run-tests.sh and the atomic-kernel-gate.sh suite. The latter is a  **Wave gate suite**  designed to ensure the entire substrate conforms to the 27H/I/J/K gate surfaces.  
* **Release Ritual:**  The release-gate.sh ensures that public API contracts and fixture semantics remain intact before a versioned release is finalized.  
* **Hash Lock Ritual:**  The lock-replay-hashes.sh script is the only authorized method for updating "golden" artifacts. Replay-hash relocking is never implicit; it is an explicit, manual ritual required when deterministic outputs are intentionally changed.Security is further reinforced by the  **Downstream Import Boundary** . Internal paths (runtime.atomic\_kernel.*) are strictly forbidden for downstream consumers; all interactions must occur through the public API (atomic\_kernel.*). This prevents internal implementation details from leaking into external dependencies.Authority is categorized into three tiers:  
* **Normative Kernel Law:**  The constitutional core (Publication I) governing all transitions and identities.  
* **Draft Extensions:**  Features like the "Lane16" parallel extension. While implemented and validated by lane16-gate.sh, they are not yet promoted to constitutional law.  
* **Advisory/Projection:**  High-level organizational layers like the "Aztec Profile" or "Metaverse Extension." These layers provide read-only views and are strictly prohibited from redefining kernel law or mutating canonical state.This enforcement plane ensures a high-integrity ecosystem where truth is established via recompute-and-verify logic, rather than unchecked environmental trust.
