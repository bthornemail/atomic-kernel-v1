# XML Propagation
Status: Advisory
Authority: Projection
Depends on: `runtime/atomic_kernel/living_xml.py`, `docs/WAVE27H_LIVING_XML_ABI.md`

Purpose: document the current enforced contract and usage boundaries for this layer.


XML surfaces are projection/runtime exchange forms over canonical contracts.

Authority remains in validated canonical artifacts and gates.

## Boundary
This layer is derived from canonical artifacts and does not redefine kernel law.
It cannot upgrade authority beyond its declared scope.
