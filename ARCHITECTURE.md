# Atomic Kernel Architecture

## Authority stack
Publication I defines canonical kernel law. Higher publications are runtime/projection/adoption surfaces and cannot redefine kernel semantics.

## Core flow
`basis -> seed -> replay -> witness -> distribution/projection`

## Determinism rules
- No nondeterministic fields in canonical artifacts.
- Same input corpus must reproduce byte-identical reports.
- Unknown keys and invalid bounds reject by default.

## Wave anchors
- Wave27H: living XML strict hierarchy + 7-cycle tick + hardening fuzz.
- Wave27I: SID/OID/CLOCK split with append-only occurrence chain.
- Wave27J: 7-bit seed closure algebra + canonical JSON companions.
- Wave27K: 16-lane parallel runtime (draft extension) with deterministic gate.
