# Atomic Kernel Foundation

`K = (S, δ, π, Σ)`

- `S`: bounded state space.
- `δ`: deterministic transition law.
- `π`: deterministic projection/witness.
- `Σ`: canonical seed derivation.

The kernel is canonical. Runtime surfaces may encode or transport state but cannot redefine law.
